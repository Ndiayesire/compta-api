import { Injectable } from '@nestjs/common';

/**
 * Placeholder pour l’envoi d’e-mails (SMTP / nodemailer).
 * Réactiver la logique historique dans ce service et brancher `MailerModule` dans `AppModule` si besoin.
 */
@Injectable()
export class MailerService {}
